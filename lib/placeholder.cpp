// Placeholder TU — cl_lib globs lib/*.cpp and CMake rejects a static library
// with no sources. Delete this once lib/ has its first real translation unit.

namespace {
[[maybe_unused]] int clLibPlaceholder_AI = 0;
}  // namespace
