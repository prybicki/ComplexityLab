// Placeholder TU — cl_engine globs engine/*.cpp and CMake rejects a static
// library with no sources. Delete this once engine/ has its first real
// translation unit.

namespace {
[[maybe_unused]] int clEnginePlaceholder_AI = 0;
}  // namespace
